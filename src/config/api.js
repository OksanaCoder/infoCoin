const API = {
    BACKEND: {
        // baseURL: process.env.REACT_APP_BACKEND_HOST,
        routes: {
            REGISTER: () => `api/register`,
            LOG_IN: () => `api/login`
       }
    }
};

export default API;
